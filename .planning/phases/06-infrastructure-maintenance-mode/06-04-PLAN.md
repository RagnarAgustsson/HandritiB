---
phase: 06-infrastructure-maintenance-mode
plan: 04
type: execute
wave: 2
depends_on:
  - 06-02
files_modified:
  - backend/app/main.py
autonomous: false
requirements:
  - INFRA-05
user_setup:
  - service: isnic.is (DNS)
    why: "CNAME record for api.handriti.is pointing to Railway"
    dashboard_config:
      - task: "Add CNAME record: api.handriti.is -> Railway custom domain hostname"
        location: "isnic.is DNS management panel"
  - service: Railway
    why: "Custom domain configuration for api.handriti.is"
    dashboard_config:
      - task: "Add custom domain api.handriti.is to the backend service"
        location: "Railway Dashboard -> Service -> Settings -> Custom Domain"
must_haves:
  truths:
    - "api.handriti.is routes to the Railway FastAPI service"
    - "handriti.is routes to Vercel (unchanged)"
    - "CORS headers allow cross-origin requests from handriti.is to api.handriti.is"
    - "Browser fetch from handriti.is to api.handriti.is/health succeeds without CORS errors"
  artifacts:
    - path: "backend/app/main.py"
      provides: "CORS middleware configured with strict origins"
      contains: "CORSMiddleware"
  key_links:
    - from: "handriti.is (browser)"
      to: "api.handriti.is (Railway)"
      via: "CORS-allowed cross-origin fetch"
      pattern: "allow_origins.*handriti"
---

<objective>
Configure subdomain routing so api.handriti.is routes to Railway and handriti.is stays on Vercel. Verify CORS allows cross-origin requests between the two. DNS and Railway custom domain require human action.

Purpose: Establishes the production routing that all subsequent phases depend on — the browser talks to Vercel for the frontend and Railway for the agent.
Output: Working CORS config, DNS instructions for user, verified cross-origin connectivity.
</objective>

<execution_context>
@/Users/ragnaragustsson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ragnaragustsson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-infrastructure-maintenance-mode/06-CONTEXT.md
@.planning/phases/06-infrastructure-maintenance-mode/06-02-SUMMARY.md
@BusinessCase/research/04-railway-deployment.md
@BusinessCase/research/05-vercel-railway-coexistence.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure CORS middleware with strict production origins</name>
  <files>backend/app/main.py</files>
  <action>
Verify and finalize the CORS middleware configuration in main.py:

1. The CORS middleware should already exist from Plan 02. Update it to be production-ready:
   - `allow_origins`: Parse from `CORS_ORIGINS` env var (comma-separated). Default: `"https://handriti.is"`
   - For dev: set `CORS_ORIGINS=https://handriti.is,http://localhost:3000` via env var
   - Do NOT use wildcard `*` in production
2. `allow_credentials=True` (needed for auth headers)
3. `allow_methods=["GET", "POST", "PUT", "DELETE", "OPTIONS"]`
4. `allow_headers=["*"]` (to accept Authorization header and custom headers)
5. Ensure the middleware is added BEFORE routers (order matters in FastAPI)

Per CONTEXT.md locked decisions:
- Strict production origins only (handriti.is, *.handriti.is)
- localhost:3000 added via env var for dev (not hardcoded)
  </action>
  <verify>
    <automated>cd /Users/ragnaragustsson/Documents/GitHub/HandritiB/backend && uv run ruff check app/ 2>&1 && uv run python -c "
from app.main import app
from starlette.middleware import Middleware
middlewares = [m for m in app.user_middleware]
print('Middleware count:', len(middlewares))
print('CORS configured')
"</automated>
  </verify>
  <done>
- CORS middleware uses strict origins from env var
- allow_credentials=True for auth headers
- No wildcard origins in default config
- localhost:3000 only included when explicitly set in CORS_ORIGINS env var
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Configure DNS and Railway custom domain for api.handriti.is</name>
  <files>n/a</files>
  <action>
User must configure DNS and Railway custom domain. Claude cannot access isnic.is or Railway dashboards.

Steps for user:
1. Railway custom domain: Go to Railway Dashboard, open the backend service, Settings -> Networking -> Custom Domain. Add `api.handriti.is`. Note the CNAME target hostname Railway provides.
2. DNS at isnic.is: Add CNAME record — Name: `api`, Type: CNAME, Value: the Railway hostname from step 1, TTL: 3600.
3. Wait for DNS propagation (5-30 minutes).
4. Verify: `curl https://api.handriti.is/health` returns FastAPI health JSON.
5. Verify CORS: Open browser console on handriti.is and run `fetch('https://api.handriti.is/health').then(r => r.json()).then(console.log)` — should succeed without CORS errors.
  </action>
  <verify>
    <automated>curl -s https://api.handriti.is/health | python3 -c "import sys,json; d=json.load(sys.stdin); print('Status:', d.get('status')); assert d.get('status') in ('ok','degraded'), 'Health check failed'"</automated>
  </verify>
  <done>
- api.handriti.is resolves and returns FastAPI health check JSON
- CORS works from handriti.is to api.handriti.is (no browser errors)
- handriti.is still routes to Vercel (unchanged)
  </done>
</task>

</tasks>

<verification>
1. `curl https://api.handriti.is/health` returns 200 with JSON body
2. `curl -H "Origin: https://handriti.is" -I https://api.handriti.is/health` includes `Access-Control-Allow-Origin: https://handriti.is`
3. Browser fetch from handriti.is to api.handriti.is/health succeeds
4. handriti.is still routes to Vercel (unchanged)
</verification>

<success_criteria>
- api.handriti.is resolves to Railway and returns FastAPI health check
- handriti.is resolves to Vercel (unchanged)
- CORS allows cross-origin requests from handriti.is to api.handriti.is
- No CORS errors in browser console when fetching api.handriti.is from handriti.is
</success_criteria>

<output>
After completion, create `.planning/phases/06-infrastructure-maintenance-mode/06-04-SUMMARY.md`
</output>
